#include <stdio.h>
#include <math.h>

int main()
{

    double value;
    double result;
    value = 25;
    result = sqrt(value);
    printf("The Square Root of %f is %f\n", value, result);

    return 0;
}
